import { MongoClient } from 'mongodb'

const test = async () => {
  const uri = 'mongodb+srv://son:son@cluster0.p71t3.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0' // Need to get the actual URI, but wait, it's local?
}
